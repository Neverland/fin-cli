/**
 * @file <% name %>.js
 * @author <% author %>(<% email %>@baidu.com)
 *
 * @since <% date %>
 */

import Vue from 'vue';

import {redirect} from 'fin-fg/util/index';

new Vue({
    el: 'body',
    methods: {
        jumpTo(url) {
            redirect(url);
        }
    }
});
